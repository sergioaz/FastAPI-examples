# PostgreSQL Database Configuration
# Copy this file to .env and update with your actual database credentials

DB_HOST=localhost
DB_PORT=5432
DB_NAME=fastapi_db
DB_USER=postgres
DB_PASSWORD=your_password_here

# Optional: Set to true for SQL query debugging
SQL_DEBUG=false
